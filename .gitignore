# Raw Hipparcos CSV (~37 MB) — not committed; see "Regenerating star data" in README
_build/hip_main.csv

# Raw HYG 4.2 catalog (astronexus.com, CC BY-SA 4.0; ~14 MB gz / ~34 MB csv)
# — not committed; the distilled _build/hyg-bayer-flam.csv is committed
_build/hygdata_v42.csv
_build/hygdata_v42.csv.gz

# Raw GeoNames gazetteer download (~6 MB zip at the repo root / ~30 MB txt)
# — not committed; the distilled js/cities.js is
/_cities5000.zip
_build/cities5000.txt

# QA scratch images (regenerable; the .mjs suites themselves are committed)
_qa/*.png

# OS / editor noise
Thumbs.db
~*
*.tmp

# Video production (local-only: capture tooling + takes)
video/

# QA dependencies (install with: npm i playwright-core in _qa/)
node_modules/

# Raw planet maps — embedded (re-encoded) in js/planets-textures.js;
# regenerate with _build/fetch-textures.mjs + _qa/embed-textures.mjs
textures/

# Minor-body texture scratch (final data-URLs live in js/planets-textures.js;
# regenerate: _qa/gen-minor-textures.mjs + _qa/normalize-minors.mjs
# + _qa/embed-minor-textures.mjs, sources documented in SOURCES.md)
_qa/_minor*.jpg
_qa/_contact-minors.html
_qa/_norm-base.html
